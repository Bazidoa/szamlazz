import { JobEnum } from "../../models/job-type";
import { Usr } from "../../models/user-interface";

export const mockUser: Usr = { id: 10, firstname: 'A', lastname: 'B', job: JobEnum.HENTES, active: true };